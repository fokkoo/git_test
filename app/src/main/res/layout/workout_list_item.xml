<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp">

    <ImageView
        android:id="@+id/workout_image"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/workout_main_text"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:text="TextView"
        app:layout_constraintStart_toEndOf="@+id/workout_image"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@tools:sample/lorem"
        />

    <TextView
        android:id="@+id/workout_secondary_text"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:text="TextView"
        app:layout_constraintStart_toStartOf="@+id/workout_main_text"
        app:layout_constraintTop_toBottomOf="@+id/workout_main_text"
        tools:text="@tools:sample/lorem" />

    />


</androidx.constraintlayout.widget.ConstraintLayout>